# Codes structure and Dependency
+ src
  + class
    + utils
    + class1
    + class2
  + rand
    + utils
  + hello
    + utils

+ Makefile
  + generate execution file try
  + OBJS for needed objects
  + ./try
